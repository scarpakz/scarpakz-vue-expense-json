{
  "name": "scarpakz-vue-expense-json",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT",
    "serve": "json-server db.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
